# copy to .env and fill in. never commit the real .env.

# aws
AWS_REGION=us-east-1
DYNAMO_TABLE=au_enrollment_stats

# rds (mysql)
RDS_USER=admin
RDS_PASSWORD=your-db-password
RDS_ENDPOINT=your-instance.xxxxxxxx.us-east-1.rds.amazonaws.com
RDS_DB_NAME=enrollment
